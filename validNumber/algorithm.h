#include <iostream>
#include <string>
//#include <boost/algorithm/string.hpp>

using namespace std;

class Solution {
public:
    bool isNumber(string s);
    string trim(string& str);
    string getNumber(string s, int *start, bool *isNumber);
    void print(string n, char expo, string n2);
};
