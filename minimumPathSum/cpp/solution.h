#include <iostream>
#include <vector>
#include <limits.h>
#include <math.h>

using namespace std;

class Solution {
    private:
        int cols, rows;
    public:
        int minPathSum(vector<vector<int>>& grid);
};
